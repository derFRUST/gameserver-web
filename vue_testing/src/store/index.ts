import Vue from 'vue'
import Vuex from 'vuex'
import Servers from './modules/servers'

Vue.use(Vuex)

export default new Vuex.Store({
  state: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    Servers
  }
})
