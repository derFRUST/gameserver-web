# source: http://[::1]:8100/graphql/
# timestamp: Sun May 10 2020 18:06:56 GMT+0200 (GMT+02:00)

schema {
  query: Query
  mutation: Mutations
}

type Game {
  id: Int!
  name: String!
  image: String!
}

type Mutations {
  startStopServer(serverId: Int!): Server!
  updateServer(serverUpdate: ServerUpdate!): Server!
}

type Query {
  games: [Game!]!
  servers: [Server!]!
}

type Server {
  id: Int!
  name: String!
  game: Game!
  status: String!
}

input ServerUpdate {
  id: Int!
  name: String!
  gameId: Int!
}
